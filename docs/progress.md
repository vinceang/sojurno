# Progress — build status & TODOs

> The living **where-are-we** of the platform build. Read this at the start of any session to find the current state, what's done, and what's open.
>
> For the *what/how* see [`architecture.md`](architecture.md); for the *why* see [`decisions/`](decisions/). This file tracks **state**, not rationale.

## Where we are

**`sojurno-v2` is the trunk** → GitHub `vinceang/sojurno` (public), deployed on Vercel at **`sojurno.com`** (custom domain; the `*.vercel.app` URL still resolves), with **Storybook hosted at `sojurno.com/storybook/`** in the same deployment. Per [ADR-0019](decisions/0019-v2-redesign-trunk-and-substrate.md), it's a fresh production rebuild that keeps the original Sojurno architecture (engine/tenant separation, five-axis tenant config, i18n, routing, capability gating) on a **shadcn/Radix + Tailwind v4** substrate. Original repo renamed `vinceang/sojurno-poc` (archive/reference); `sojurno-redesign` = visual reference only.

**Phases A–B merged; Phase D (page composition) largely done; Phase C (Figma round-trip) nearly complete — C0–C4 authored.** The product surface is built and live (landing, communities, explore w/ filter bar + grid/list, listing detail w/ mosaic gallery + date-range picker, host, about, **Collections** rail + detail per ADR-0020). `typecheck`/`lint`/`build` clean; CI green per PR. See the Figma build status below.

## Product spine — re-implemented in v2

The original repo's five phases all have working analogues in v2 (this is config/architecture, not pending work):

| Capability | ADR(s) | State in v2 |
| --- | --- | --- |
| Routing spine — deep-linkable `/t/:tenant/...` | 0006 | ✅ React Router routes live |
| Tenant config + branded app — five-axis typed config, `data-tenant`×`data-theme` | 0002 / 0014 / 0015 | ✅ `src/tenants/`; accent re-points per tenant; **no community-name branching** (verified) |
| Platform shell — `/` landing, `/communities`, `/start`, `/about` | 0015 | ✅ public shell + `CommunityListingRow` rows + 4-col footer |
| Traveler/host split | 0006 / 0021 | ✅ **ADR-0021 built** — `role/`→`theme/` rename; persisted mock `SessionProvider` (`localStorage`); "Become a host" onboarding form (zero validation) → demo modal → authenticated host session; fake login screen; session-aware ProfileMenu (anonymous → become-host/login; authed → host dashboard / switch mode / sign out); Host nav + dashboard identity gated on session. |
| Gear module — capability-gated | 0003 / 0004 / 0005 | ✅ `gear` capability on hikers, off runners; data + types present |
| Collections — capability-gated discovery rail | 0020 | ✅ `collections` capability on runners + hikers; rail atop Explore + detail pages |
| Design-system launchpad — `/design` → Storybook | 0007 | ✅ `/design` launchpad + About card → **hosted Storybook at `/storybook`** (`STORYBOOK_URL`; ADR-0007 seam closed) |

## v2 roadmap (ADR-0019 phasing — kit before pages)

| Phase | Focus | Status |
| --- | --- | --- |
| A | **Foundation** — reinstate DTCG → Style Dictionary over the `@theme inline` bridge (`_tokens.scss` becomes generated); add GitHub Actions CI | ✅ Done (PR #1 merged to `main`; CI green; deployed to Vercel) |
| B | **Base component library** — shadcn primitives themed via tokens, documented cva variants, Storybook + a11y per component. Split: **B1** conventions + harden existing 5 primitives, **B2** wrap in-use Radix (DropdownMenu/Tabs/Checkbox) + Card/Input/Separator, **B3** Select/Switch/Tooltip | ✅ Done (B1–B3; branch `phase-b-component-library`) — 15 documented primitives, story + a11y each |
| C | **Figma round-trip** — spec (C0) + componentization (C1) + Storybook coverage (C2) + Figma library (C3) + Code Connect (C4) | 🔧 Nearly done — C0–C4 all authored. Figma file has Foundations + 15 components; 16 `.figma.tsx` Code Connect mappings in repo. **Publishing to Dev Mode is plan-gated** (Code Connect needs Org/Enterprise; account is Pro) — mappings are publishable as-is once upgraded. |
| D | **Page composition** — full visual parity + Collections | ✅ Largely done — landing/communities/explore/listing/host/about + Collections rebuilt to the redesign; minor polish remains |
| E | **Content & imagery** — swap Unsplash for AI-generated static assets (ADR-0016) | 🔧 In progress — generated listings carry AI images (OpenAI `gpt-image-1` → Supabase Storage); seed 12 + avatars stay on Unsplash (owner-approved interim; backfill deferred — risks image↔copy drift) |
| F | **Backend persistence + generator** (ADR-0022, promotes ADR-0017) — Supabase Postgres + Storage, offline OpenAI listing generator, build-time export into the app | ✅ Built & run live — migration applied, seed 12 migrated, 17 listings generated (incl. 8 Climbers) with `gpt-image-1`; DB is the source of truth via the committed `listings.generated.ts` export. Auth/admin + runtime reads stay parked. |

Status legend: ⬜ not started · 🔧 in progress · ✅ done · ⚠️ needs attention · ⏸ blocked.

**Phase done = all true:** `npm run build` passes · `npm run typecheck` clean · `npm run lint` clean · Storybook a11y audit passes for any new/changed component · the engine/tenant line is intact · committed at the boundary referencing the ADR(s).

## Figma build status (Phase C) — spec: [`design-system.md`](design-system.md)

- **Figma file:** `A3mcfYDq5wZ7As46yri9jO` ("Sojurno Design System").
- **C0 spec** ✅ · **C1 componentization** ✅ (Eyebrow/SectionHeader extracted; CommunityCard merged) · **C2 Storybook coverage** ✅ (hosted at `/storybook`).
- **C3 Figma library** ✅ — Foundations: 51 variables across 4 collections (Primitives `Value`; Color `Light`/`Dark`; Radius `Value`; Community `Runners`/`Hikers`), all scoped + WEB code syntax; 9 text styles; 3 shadow styles; a Foundations specimen page. 15 components built one-per-page with token-bound variants (Button, Badge, Avatar, Eyebrow, Separator, Input, Card, Rating, Stepper, SectionHeader, Checkbox, Switch, Tabs, Select, Tooltip, Dialog). **Calendar deliberately not modeled** (vendored `react-day-picker` widget).
- **C4 Code Connect** ✅ authored — 16 `src/lib/*.figma.tsx` parser files (`figma.connect`, type-checked against component props), `figma.config.json`, `@figma/code-connect` dep, `figma:parse`/`figma:publish` scripts. `npx figma connect parse` validates all 16 with zero errors. **Publishing to Figma Dev Mode is plan-gated** — Code Connect requires Org/Enterprise; the account is Pro. The mappings are valid, version-controlled artifacts and become live with `npm run figma:publish` once on Org/Enterprise (no rework).

## Open TODOs & carried-forward seams

- ~~**[Phase A / tokens]** Reinstate the **DTCG → Style Dictionary** pipeline so `src/styles/_tokens.scss` becomes generated output.~~ ✅ Done on `phase-a-tokens`: `tokens/*.json` (primitives + semantic light/dark + per-tenant) → `tokens/build-tokens.mjs` → generated `src/styles/_tokens.scss` (git-ignored, built by `npm run tokens`, auto-run via `pre*` hooks). `[data-community]` tints now reference token primitives (hex lives only in `primitives.json`). **Still open:** the Figma-variable export (with light/dark/tenant modes) for the Code Connect round-trip — lands in Phase C.
- ~~**[Phase A / CI]** Add **GitHub Actions** running `typecheck → lint → build → storybook a11y` per PR.~~ ✅ Done: `.github/workflows/ci.yml` (verified locally; first remote run on push/PR).
- ~~**[Phase C]** **Figma Code Connect** — `.figma.tsx` per component + Figma library.~~ ✅ Authored (16 mappings + Figma library). **Open:** publish to Dev Mode (plan-gated — needs Org/Enterprise; run `npm run figma:publish` once upgraded). Calendar not modeled (vendored widget).
- ~~**[Role seam → ADR-0021]** rename `role/`→`theme/`; session-gated mock host onboarding.~~ ✅ Built. **Open seam (explicit in ADR-0021 §5):** traveler mock identity (so reservation requests have a "from") — undecided, revisit when load-bearing.
- **[Imagery]** **Partly resolved (ADR-0022):** generated listings now carry **AI images** (OpenAI `gpt-image-1` → Supabase Storage) — the ADR-0016 path for new content. The **seed 12** still render **Unsplash** (`ListingCard` `<img src={listing.images[0]?.src}>`) as the owner-approved interim; backfilling them is optional. No `Scene` fallback in v2 (the original's generative `Scene` was not carried over).
- ~~**[Storybook]** Document all primitive + composite variants/states.~~ ✅ Every primitive has a story (cva ones show full variant matrices); composites now covered too — ListingCard, ListingRow, CollectionCard, CommunityCard, CollectionRail, ExploreFilterBar, SearchPanel, CommunityListingRow. Global preview decorator wraps stories in router + i18n. Hosted at `/storybook`.
- **[Visual parity]** Full-bleed photographic hero, darker community-builder module, richer Explore controls, listing detail gallery, host dashboard refinements (per `v2-build-note.md`).
- **[Search/dates]** Decide whether the landing search/date controls stay lightweight (demo) or become real interactive inputs.
- **[Post-v1]** Per-tenant **typography** (curated font pairings) — gated on a font loading/performance decision (ADR-0014 seam).
- **[Generator]** Separate AI listing-asset generator app (own repo) emitting to the canonical `Listing` schema.
- **[Profile]** `/t/:tenant/profile` route is intentionally **omitted** until a profile page is specced.
- **[QA]** No browser automation installed (Playwright absent); responsive visual QA is manual/user-assisted.
- **[Housekeeping]** Decide whether `.claude/` should be git-ignored or tracked.

## Decision log pointers

- **Accepted, implemented (foundation):** **v2 as trunk + redesign visual language + shadcn/Tailwind substrate** (ADR-0019, supersedes the *visual layer* of ADR-0014, revisits ADR-0007's substrate). `sojurno-v2` is trunk; Instrument Serif + Plus Jakarta Sans; neutral base + community **accent**; retire "arch" radius. Substrate **shadcn/Radix + Tailwind v4** with tokens fed via `@theme inline`. Tokens are **interim hand-authored SCSS**; **DTCG → Style Dictionary pipeline scheduled for reinstatement** (anchors the Figma round-trip). i18n **EN/ES/FR**, globe-icon dropdown (built). **Unsplash imagery** is an owner-approved interim deviation from ADR-0016. Code Connect + CI pending. Trunk analysis: [`redesign-decision.md`](redesign-decision.md).
- Accepted: **tenant brand axis = logo + color + typography**, bounded to token slots (ADR-0014, extends ADR-0002); typography shipped as a seam. ADR-0019 supersedes its visual *values*; its governance principle stands.
- Accepted: **listing content model** (ADR-0016). Listings may carry **AI-generated photorealistic images** made offline, no runtime AI, no stock/scraped. **Marketplace content single-language**; chrome bilingual. *v2 interim:* Unsplash imagery is used until the generator lands (recorded in ADR-0019 as a tracked deviation).
- Accepted: **platform shell vs tenant app** (ADR-0015, extends ADR-0006). `/` landing + community gallery, `/communities`, `/start` informational; reservations stay in the tenant flow; two header contexts.
- **Accepted, built (read path refined):** **persistence & backend** (ADR-0017, promoted by **ADR-0022**) — Supabase Postgres + Storage, tenant RLS, generator-as-offline-preloader. **Built:** schema (`tenants`, `listings`), an offline **OpenAI** generator (`gpt-image-1` images → Supabase Storage; structured-output copy), and a **build-time export** into the committed `src/data/listings.generated.ts` (the app reads synchronously — no runtime DB dependency; flipping to runtime `supabase-js`+RLS stays a one-file change). **Still parked:** auth/admin; runtime reads. The dormant data-fetching best-practice categories activate only when runtime reads land.
- **Accepted, built:** **Toast notifications, login-gated save & in-context login modal** (ADR-0025, revises ADR-0023's frictionless heart). Singleton toast pattern (Radix Toast); Save gated behind login with an in-context modal (saves complete after sign-in as Test User + success toast); legal disclaimer surfaces as a toast; **Saved view** at `/saved` (grouped by community, reached from the account menu). WS1 (toast) + WS2 (gated save + modal) + WS3 (Saved view) all built.
- **Accepted, built:** **Mock credentialed login + Test User identity** (ADR-0024, revises ADR-0021 §3). A real-looking `/login` form with empty-field-only validation; valid submit always signs in as the fixed `Test User` identity (the canonical owner for the future listing editor + Saved view). Forgot password = demo affordance (ADR-0023); Create an account = onboarding; signed-in state hides the login button and prefills the onboarding name.
- **Accepted, built:** **No dead-end CTAs** (ADR-0023, generalizes ADR-0021's demo-mode pattern). Every major CTA resolves to a visible response via one of three tiers: demo confirmation `Dialog` (transactional), real/concept page (navigational), or a lightweight inline stub (minor controls). **WS1+WS2** shipped the footer **legal pages** as real long-form content (`data/legal.ts` per-locale + EN fallback; `.sj-prose`). **WS3** shipped the reusable `DemoActionDialog` (+ `useDemoAction`), refactored `BecomeHostPage` onto it, and wired Reserve / Accept-Decline / Create-a-community + the Explore "Any dates" stub. Add/edit-listing explicitly out of scope.
- **Accepted, built:** **backend persistence + OpenAI listing generator** (ADR-0022, promotes ADR-0017). Generator = OpenAI for both copy (structured outputs) and imagery (`gpt-image-1`, closing the ADR-0016 seam for generated listings → Supabase Storage; seed 12 stay on Unsplash). Scope deliberately bounded to data + generator + storage + export (no auth). Triggered by the 6-up landing rows needing more than 6 listings/tenant.
- **Accepted, built:** **Collections discovery rail** (ADR-0020, promotes & generalizes ADR-0009). A `collections` capability + generic `Collection` (`kind: event | place`); tenant-scoped curated groupings (runner races, hiker trails/parks) shown as a horizontal poster rail atop Explore, each linking to a detail page of associated listings. Engine/tenant-clean; mock data (ADR-0017 stays parked). Supersedes ADR-0009's Event→Collection shape; host-run event *mechanics* remain out of scope.
- **Proposed:** **linked listings** (ADR-0018) — host links their **own** external listing (`bookingMode` + `externalUrl`), booking is an outbound link, host-entered data only (no scraping). Needs acceptance, then a build phase.
- Proposed / not-in-v1: **iCal availability sync** (ADR-0012). Rejected: **external-listing ingestion** (ADR-0013), **events** (ADR-0009), **AI intent search** (ADR-0010). See [`AGENTS.md`](../AGENTS.md) §8.

## Changelog (recent first)

- **2026-06-20** — **Saved view — WS3 (ADR-0025, completes it)** on `feat/saved-view`. New `/saved` page listing the signed-in Test User's hearted stays, **grouped by community** (runners/hikers/climbers), rendered with `ListingCard`. Reached from the **account menu** (desktop `ProfileMenu` + mobile menu) when authenticated. Graceful states: anonymous → log-in prompt; signed-in with none → empty state with a "browse stays" CTA. Exposes `savedIds` from `SavedListingsProvider` via a new `useSaved()` hook; unsaving a card updates the list live. New `saved.*` + `account.saved` keys ×3 locales. typecheck/lint/build/build-storybook green. ADR-0025 fully built (toast system + gated save + login modal + Saved view).

- **2026-06-20** — **Gated save + in-context login modal — WS2 (ADR-0025)** on `feat/gated-save-login-modal`. The Save (heart) action is now **gated behind login** (revises ADR-0023's frictionless heart): clicking it while anonymous opens a **login modal** (`LoginDialog`) so you stay on the page; after sign-in (as Test User) the **pending save completes** and a **success toast** fires. Saving while signed in saves + toasts immediately; **unsaving is silent**. Refactored `useSavedListing` into a context-backed `SavedListingsProvider` (hearts now sync live across a page). Extracted shared `LoginForm` (used by both `/login` and the modal); `LoginPage` recomposed onto it. Storybook preview decorator wrapped in Session/Toast/Saved providers so `ListingCard` stories still render. New `save.*` keys ×3 locales. typecheck/lint/build/build-storybook green. **Next (WS3):** the Saved view (Test User's saved stays).

- **2026-06-20** — **Toast notification system — WS1 (ADR-0025)** on `feat/toast-system`. New transient toast pattern: themed Radix Toast primitive (`lib/Toast`, `@radix-ui/react-toast` within ADR-0019's Radix set) + app-level `ToastProvider`/`useToast` (singleton — a new toast replaces the current and resets its timer, so a gallery of hearts can't stack toasts; auto-dismiss + close button + swipe; **scoped to the firing page** so it never outlives it; tones default/success/info). First adoption: the legal pages' "sample content" disclaimer moved from an inline banner to an **info toast on entry**. Storybook story added. typecheck/lint/build/build-storybook green. **Next (WS2):** gate Save behind login + in-context login modal + save-success toast; then the Saved view.

- **2026-06-20** — **Mock credentialed login + Test User identity (ADR-0024)** on `feat/mock-login`. Replaced the ADR-0021 login stub with a real-looking form (username, password, Forgot password?, Create an account). Validation scheme = **empty-field rejection only** (inline errors, `aria-invalid`); any non-empty values pass. Submit **always signs in as the fixed `Test User` identity** (overrides ADR-0021 §3's re-entry semantics) — the canonical owner for the future add/edit-listing facility and the upcoming Saved view. Forgot password → `DemoActionDialog` (ADR-0023); Create an account → onboarding. When already signed in, `/login` shows a "signed in as Test User" note (no login button) and the onboarding "Your name" field prefills with the active identity. New `login.*` keys ×3 locales; obsolete `login.becomeHost` removed. typecheck/lint/build green. **Next:** the Saved view.
- **2026-06-20** — **Sojurno wordmark logo** on `feat/brand-logo`. Replaced the text wordmark in `Brand` (header + footer) with the real SVG logo (`Wordmark` component). Letter glyphs use `currentColor` so the single asset adapts to light/dark via the `foreground` token — no separate "reverse" file; the dot on the "j" is a new fixed brand-mark red (`--color-brand-mark` = `#ff0505`, added to `tokens/primitives.json` + bridged in `tailwind.css` → `fill-brand-mark`, honoring the no-raw-hex-in-components rule). typecheck/lint/build green.
- **2026-06-19** — **No dead-end CTAs — WS3 (ADR-0023)** on `feat/demo-cta-affordances`. Reusable `DemoActionDialog` (+ `useDemoAction` hook in its own file for the react-refresh rule) — acknowledge mode (single dismiss) or confirm mode (`onConfirm` → Cancel/Confirm). Refactored `BecomeHostPage` onto it (removed the duplicated inline dialog). Wired the remaining dead CTAs: ListingPage **Reserve** ("Request sent"), HostDashboard **Accept/Decline**, StartPage **Create a community**, and the ExploreFilterBar **"Any dates"** pill (the actually-dead control — the Selects/toggles were already wired). i18n EN/ES/FR for all. Two layout tweaks rode along: **Explore filter bar moved directly below the hero, above the Collections rail**; and a **"← Back to [tenant]"** link at the foot of each listing detail (mirrors the legal pages' "← Back to home"). typecheck/lint/build green.
- **2026-06-19** — **No dead-end CTAs — WS1+WS2 (ADR-0023)** on `feat/legal-content-pages` (PR #31). Established the convention that every major CTA resolves to a visible response (generalizing ADR-0021's demo-mode pattern). First slice: the footer's **Privacy / Terms / Accessibility** links become **real long-form content pages** at `/legal/:docId` (unknown ids redirect home). Content is clearly-marked sample boilerplate in a new per-locale data module `src/data/legal.ts` (full EN/ES/FR, `getLegalDoc` with EN fallback) — kept out of `messages.ts` to avoid forcing 3-locale parity on multi-paragraph prose (honors ADR-0016: chrome bilingual, body per-locale). New reusable `.sj-prose` reading layout exercises the Instrument Serif → Plus Jakarta hierarchy (tokens only). Footer wired; `legal.*` chrome keys ×3 locales. typecheck/lint/build green. **Next (WS3):** extract `DemoActionDialog`, wire Reserve / Accept-Decline / Create-a-community + the Explore filter stub.
- **2026-06-19** — **Hero backdrop: tri-tone aura + grain** (PRs #28→#30). Gave the landing hero atmosphere: a soft aura blending the three active community accents (ember/pine/violet) so it reads as the multi-tenant *engine*, not one discipline — built with `color-mix` over tokens (no raw hex). First iteration layered faint topographic contour lines; revisited and replaced them with a subtle `feTurbulence` film-grain (`<HeroBackground>`, `texture` prop) after the contours read as wallpaper. Pure CSS/SVG, $0, no external assets — deliberately chosen over a photographic/isometric hero (would brand the platform as one community) and over Higgsfield (reserved for a different project; this repo's image pipeline is OpenAI).
- **2026-06-19** — **Footer "Start hosting" fix** (PR #27). Pointed the footer CTA at `/become-a-host` (the onboarding view) instead of the gated `/t/runners/host` dashboard, and hid it when a host is authenticated (redundant once signed in). Uses `useSession`.
- **2026-06-19** — **Listing gallery photo variety** (PR #26). `getGallery` filled the detail-page mosaic with other listings' hero images in fixed array order, so every listing showed the same supporting photos. Rotate the filler window by a stable hash of the listing id → each page shows a distinct set. Deterministic per id.
- **2026-06-19** — **CLAUDE.md rewritten to v2 reality** (PR #29). The briefing still described the original single-file `App.tsx` prototype (pnpm, no backend/router, inline hex). Rewrote it as a concise orientation that **defers to the living docs** (`AGENTS.md`, `architecture.md`, `decisions/`, `progress.md`) instead of restating drift-prone specifics; corrected stack, file tree, routes, and the conventions that bite (engine/tenant line, tokens-only, DB export seam, green-bar, PR-per-change).
- **2026-06-19** — **Climbers community activated + populated** on `feat/backend-openai-generator`. Proves the multi-tenant thesis end-to-end via config + data only (no community-name branching): widened `ActiveTenantId` to include `climbers`; flipped `active: true` + `capabilities: ['gear']` + real `stats`; added `CLIMBER_GEAR` (rope/harness/helmet/crash pad/quickdraws — new gear icon enum values, data-only) + climbers `REVIEWS` + a landing community row + EN/ES/FR `landing.climbers*` keys + a generator `TENANT_BRIEF`. Generated **8 climbers listings** with `gpt-image-1` images (Smith Rock, New River Gorge, Red River Gorge, Joshua Tree) — style held across the new domain with no prompt changes. App now serves **3 communities, 29 listings** (runners 11 / hikers 10 / climbers 8). typecheck/lint/build green.
- **2026-06-19** — **Seed 12 migrated into Supabase — DB is single source of truth.** Backfilled the hand-authored seed listings (`source='seed'`); export now pulls ALL published rows into the committed `listings.generated.ts` (renamed `GENERATED_LISTINGS` → `DB_LISTINGS`). The app reads every listing from the DB export; the in-code `SEED_LISTINGS` array is retained only as the re-backfill source (tree-shaken from the bundle). Linked external demos preserved.
- **2026-06-19** — **Phase F kickoff: backend persistence + OpenAI generator (code built)** on `feat/backend-openai-generator` (→ **ADR-0022**, promotes ADR-0017). Triggered by the 6-up landing rows needing >6 listings/tenant. Built the offline pipeline: `supabase/migrations/` (`tenants` + `listings` tables mirroring the `Listing` type, RLS published-only reads, public `listing-images` Storage bucket); `scripts/generate-listings.ts` (OpenAI **structured outputs** for copy + **`gpt-image-1`** for a hero image → Supabase Storage → service-role bulk insert; cost estimate + `--dry-run`); `scripts/export-listings.ts` → committed `src/data/listings.generated.ts`. `src/data/listings.ts` is now the seam (`LISTINGS = seed + generated`, accessors unchanged → **zero page changes**). Read path is **build-time export** (app stays synchronous/static; runtime `supabase-js`+RLS reads remain a one-file future flip — see ADR-0022 §3). Secrets (`.env`, git-ignored) are scripts-only; `.env.example` added; ESLint `scripts/**` Node-globals override. typecheck/lint/build green. **Pending live run:** Supabase project + OpenAI key, apply migration, first `gen:listings` + `export:listings`.
- **2026-06-19** — **Landing cards: 6-up rows + fluid compact card** on `landing-card-scaling`. CommunityListingRow desktop item width → 6-up (sm:2 / md:3 / lg:6); the carousel scrolls past 6. ListingCard compact is now a **container** (Tailwind `@container`) and its type scales fluidly to the card's own width via `clamp(min, cqi, max)`, with the sizes as CSS-variable tokens (`--lc-title/meta/price` in `.listing-card-compact`) — one place to tune the scale, no per-layout hand-tuning. Establishes the forward styling principle: **values = CSS-variable tokens; container queries = the tool for container-responsive components (the card family)**. (Only 6 listings/tenant exist today, so the row is exactly full until the ADR-0017 generator adds more.) typecheck/lint/build green; container-type + cqi confirmed in the bundle.
- **2026-06-19** — **Custom domain live:** Sojurno is served at **`sojurno.com`** (Vercel custom domain; `*.vercel.app` still resolves). Hosted Storybook at `sojurno.com/storybook/`. Swept current-state docs (`progress.md`, `project-context.md`) to the canonical domain; dated changelog entries keep their historical URLs.
- **2026-06-19** — **Composite Storybook stories** on `composite-stories`. Added stories for the composite components (ListingRow, CollectionCard, CommunityCard, CollectionRail, ExploreFilterBar, SearchPanel, CommunityListingRow) so the hosted Storybook documents the full system, not just primitives. Added a global preview decorator (`.storybook/preview.tsx`) wrapping every story in `MemoryRouter` + `I18nProvider` so composites using `<Link>`/`useNavigate`/`useI18n` render; simplified `ListingCard.stories` to drop its now-redundant local router. typecheck/lint/build/build-storybook all green (a11y audit passes on every story).
- **2026-06-19** — **Dark mode shipped** on `dark-mode`. Added a `ThemeToggle` (sun/moon) to the header (desktop + mobile) calling the existing `theme/useTheme` `toggleTheme`; `data-theme` flip + `localStorage` persistence already existed, so the dark token layer (`[data-theme="dark"]`) now lights up across the app. Audit found just **one** hardcoded light-only color in the whole app (the landing `01/02/03` step numbers) — fixed to `text-foreground/15` so it re-themes; the 4 `from-black` gradients are intentional (white text over images). i18n EN/ES/FR. typecheck/lint/build green.
- **2026-06-19** — **ADR-0021 built (mock host onboarding + session-gated role)** on `host-onboarding`. Renamed `src/role/` → `src/theme/` (it held the theme provider — fixes the long-standing naming collision; no other consumers). New `src/session/` — persisted mock `SessionProvider`/`useSession` (`localStorage`, survives refresh; sign-out keeps the identity so re-login works). New `/become-a-host` onboarding form (zero validation by design) → demo-mode `Dialog` → creates an authenticated host session and lands on the host dashboard; new `/login` fake login that re-enters the persisted identity. Header's static Sign-in/Start-hosting buttons replaced by a session-aware `ProfileMenu` (anonymous → become-host/login; authed → host dashboard / switch hosting↔traveling / sign out); Host nav item + dashboard identity row gated on an active session; mobile hamburger mirrors the account states. Added `DEFAULT_TENANT` export. i18n EN/ES/FR. Traveler mock identity left as an explicit seam (ADR-0021 §5). typecheck/lint/build green.
- **2026-06-19** — **Phase C / C3 + C4 — Figma library + Code Connect.** Built the Figma design-system file (`A3mcfYDq5wZ7As46yri9jO`): Foundations (51 variables, 9 text + 3 shadow styles, specimen page) + 15 token-bound component pages. Authored **16 `src/lib/*.figma.tsx` Code Connect mappings** (`figma.connect`, type-checked against component props) + `figma.config.json` + `@figma/code-connect` dep + `figma:parse`/`figma:publish` scripts; `figma connect parse` validates all 16. Publishing to Dev Mode is plan-gated (Code Connect needs Org/Enterprise; account is Pro) — mappings publishable as-is once upgraded. ESLint exempts `*.figma.tsx` from the Fast-Refresh rule. Calendar not modeled (vendored widget). Also brought this file current (it had drifted: trunk facts, Storybook hosting at `/storybook`, page-composition passes, Collections, ADR-0021 pointer).
- **2026-06-18** — **Phase C kickoff: componentization (C0 + C1)** on `design-system` (→ ADR-0019, [`design-system.md`](design-system.md)). Wrote the design-system spec (layering, variant-naming conventions that become Figma variant names, primitive inventory, extraction audit, Figma mapping, C0–C4 sequence). C1 extractions: new `Eyebrow` + `SectionHeader` primitives (stories + barrel), adopted across all pages (replacing 14 hand-rolled eyebrows + the per-page header clusters); merged `UpcomingCommunityCard` into one `CommunityCard` with active/upcoming states; folded stray chips into `Badge`. `MediaTile` + broad `Card` adoption deliberately deferred (documented). Next: C2 Storybook variant coverage → C3 Figma library → C4 Code Connect. typecheck/lint/build green.


- **2026-06-18** — **Collections discovery rail built** on `collections` (→ ADR-0020, which promotes & generalizes ADR-0009). New `collections` capability (on for runners + hikers); generic `Collection` type with `kind: event | place`; mock `COLLECTIONS` (runner races, hiker trails/parks) associating existing listings. New `CollectionCard` (poster-style overlaid tile; accent date chip for events), `CollectionRail` (snap-scroll, label from `vocabulary.collectionsLabel`) at the top of Explore (capability-gated), and `CollectionDetailPage` at `/t/:tenant/collections/:id` (serif hero + filtered listings via `ListingCard`). Engine/tenant-clean (no name branching). Mock data; ADR-0017 persistence stays parked. i18n EN/ES/FR. typecheck/lint/build green. Designed with the `frontend-design` skill (poster tiles distinct from product cards). Pending: review/merge.


- **2026-06-17** — **Phase B3 (remaining primitives)** on `phase-b-component-library` (→ ADR-0019). Added `Switch`, `Tooltip` (+Provider/Trigger/Content), and `Select` (Trigger/Value/Content/Item) themed wrappers + Storybook stories (a11y:error). Installed `@radix-ui/react-{select,switch,tooltip}` (within ADR-0019's pre-approved Radix category). Additive/documented — no app consumers yet, so they tree-shake out of the app bundle (unchanged at ~367 kB). **Phase B complete: 15 documented primitives, a story + a11y per component.** Next: Phase C (Figma round-trip).
- **2026-06-17** — **Phase B2 (Radix wrappers + card consolidation)** on `phase-b-component-library` (→ ADR-0019). **B2a:** themed wrappers around the in-use Radix primitives — DropdownMenu, Tabs, Checkbox, Dialog — each with a Storybook story (a11y:error); refactored the raw usages (CommunityMenu + LanguageSwitcher → DropdownMenu, consolidating duplicated chrome; HostDashboard → Tabs; ListingPage gear → Checkbox); made `Button` forwardRef for `asChild` composition. **B2b:** `Card` (cva padding/elevation + asChild), `Input`, `Separator` primitives + stories; refactored the repeated `rounded-xl border bg-card p-5 shadow-sm` pattern (Metric, ReservationCard, Panel, sticky aside) onto `Card`. Wrappers are plain function components so `react-refresh/only-export-components` stays clean. typecheck/lint/build/storybook all green. B3 (Select/Switch/Tooltip — need new Radix deps) pending.
- **2026-06-17** — **Phase B1 (component-library conventions)** on `phase-b-component-library` (→ ADR-0019). Locked the primitive convention (cva variants + token-backed Tailwind + `cn`); converted `Badge` (tone variants) and `Avatar` (size variants + optional image) to cva; added Storybook stories with `a11y: { test: 'error' }` for Avatar, Rating, Stepper (Button/Badge already had them); added `src/lib/index.ts` as the side-effect-free public-API barrel (components only — utils stay direct per the react-refresh rule). typecheck/lint/build/storybook all green. B2 (wrap in-use Radix + Card/Input/Separator) and B3 (Dialog/Select/Switch/Tooltip) pending.
- **2026-06-17** — **Phase A merged** (PR #1 → `main`); CI green; v2 deployed to Vercel (`sojurno.vercel.app`) after repointing the existing Vercel project to the new repo. Original repo renamed `sojurno-poc`; fresh public `vinceang/sojurno` is the trunk.
- **2026-06-17** — **Phase A (Foundation) built** on `phase-a-tokens` (→ ADR-0019). Reinstated the DTCG → Style Dictionary token pipeline: `tokens/*.json` (primitives + semantic light/dark + 4 per-tenant accent overrides) compiled by `tokens/build-tokens.mjs` into a git-ignored `src/styles/_tokens.scss`, consumed by the existing Tailwind `@theme inline` bridge — values reproduce the prior hand-authored tokens exactly (no visual regression). `[data-community]` tints re-pointed to token primitives (hex now lives only in `primitives.json`). Added `npm run tokens` + `pre*` hooks (dev/build/storybook) and a GitHub Actions CI workflow (`typecheck → lint → build → storybook a11y`); all four green locally. Figma-variable export (with modes) deferred to Phase C.
- **2026-06-17** — **Docs reconciled to v2 reality.** Added [ADR-0019](decisions/0019-v2-redesign-trunk-and-substrate.md) (v2 as trunk; shadcn/Tailwind substrate; redesign visual language; DTCG reinstatement + Code Connect + CI scheduled; EN/ES/FR; Unsplash interim). Flipped `redesign-decision.md` to RESOLVED. Rewrote this file's phase model and open seams to match v2. Updated `architecture.md` and `AGENTS.md` to the v2 substrate (Tailwind/Radix/cva, interim SCSS tokens, EN/ES/FR, Unsplash interim).
- **2026-06-17** — v2 redesign implementation pass on `feat/v2-production-redesign`: fresh React/Vite production build keeping Sojurno's affinity/multi-tenant thesis while translating the Figma Make redesign into production patterns. Real routes, EN/ES/FR chrome, tenant-scoped listing data, public header/footer, `/about` maker page, reusable `CommunityListingRow` landing sections, builder CTA strip. Consolidated primary CTA styling through the `Button` primitive after finding `Button asChild` links lost contrast to the global anchor reset. State: promising but incomplete (visual parity, Storybook coverage, search/date behavior, richer Explore/detail/host views, browser QA all open). See [`v2-build-note.md`](v2-build-note.md).

> Historical changelog for the original `sojurno` repo (Phases 1–5, June 14–16) lives in that repo's `docs/progress.md`; v2 re-implements that spine rather than continuing its branch/PR history.
